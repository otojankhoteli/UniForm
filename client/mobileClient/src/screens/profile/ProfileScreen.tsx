import React from "react";
import { View } from "react-native";

const ProfileScreen: React.FC = () => {
  return (
    <View style={{ flex: 1, backgroundColor: "rgba(255, 128, 64, 1)" }}></View>
  );
};

export default ProfileScreen;
